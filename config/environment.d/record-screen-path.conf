# Prepends the user proxy directory to PATH so Hyprland/Quickshell bar calls
# to omarchy-capture-screenrecording use wf-recorder instead of
# gpu-screen-recorder, which crashes the GPU (VCE encoder).
PATH=${HOME}/.config/omarchy/bin:${PATH}